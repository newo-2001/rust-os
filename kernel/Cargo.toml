[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"

[[bin]]
name = "kernel"
test = false
bench = false

[dependencies]
num_enum = { version = "0.7.6", default-features = false }
libkernel = { path = "../libkernel" }
log = "0.4.34"

[build-dependencies]
cc = "1"

[lints]
workspace = true
